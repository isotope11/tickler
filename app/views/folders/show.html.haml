.folder
  %h2= "Folder: #{@folder_name}"
  .cards
    %ul
      - @cards.each do |card|
        %li= link_to card, card
    %h3 Add new card
    = render partial: 'cards/form', locals: { card: @card }
